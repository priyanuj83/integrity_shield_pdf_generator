\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\geometry{margin=1in}
\begin{document}
\begin{center}
Machine Learning - Graduate Level Assessment 2 \
Total Marks: 40 \
Answer all questions.
\end{center}

\section*{Multiple Choice (10 marks)}
\begin{enumerate}[label=\arabic*.]
\item Suppose we like to calculate P(H|E, F) and we have no conditional independence information. Which of the following sets of numbers are sufficient for the calculation?
\begin{enumerate}[label=(\alph*)]
    \item P(E, F), P(H), P(E|H), P(F|H)
    \item P(E, F), P(H), P(E, F|H)
    \item P(H), P(E|H), P(F|H)
    \item P(E, F), P(E|H), P(F|H)
\end{enumerate}
\item Consider the Bayesian network given below. How many independent parameters would we need if we made no assumptions about independence or conditional independence H -\textgreater{} U \textless{}- P \textless{}- W?
\begin{enumerate}[label=(\alph*)]
    \item 3
    \item 4
    \item 7
    \item 15
\end{enumerate}
\item The number of test examples needed to get statistically significant results should be \_
\begin{enumerate}[label=(\alph*)]
    \item Larger if the error rate is larger.
    \item Larger if the error rate is smaller.
    \item Smaller if the error rate is smaller.
    \item It does not matter.
\end{enumerate}
\item Which of the following best describes the joint probability distribution P(X, Y, Z) for the given Bayes net. X \textless{}- Y -\textgreater{} Z?
\begin{enumerate}[label=(\alph*)]
    \item P(X, Y, Z) = P(Y) * P(X|Y) * P(Z|Y)
    \item P(X, Y, Z) = P(X) * P(Y|X) * P(Z|Y)
    \item P(X, Y, Z) = P(Z) * P(X|Z) * P(Y|Z)
    \item P(X, Y, Z) = P(X) * P(Y) * P(Z)
\end{enumerate}
\item Adding more basis functions in a linear model, pick the most probably option:
\begin{enumerate}[label=(\alph*)]
    \item Decreases model bias
    \item Decreases estimation bias
    \item Decreases variance
    \item Doesn't affect bias and variance
\end{enumerate}
\end{enumerate}

\section*{True / False (10 marks)}
\textit{Answer True or False}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{5}
\item True or False: The temperature of a nonuniform softmax distribution does not influence its entropy in any significant manner.
\item True or False: DenseNets typically require more memory than ResNets due to their dense connections and feature reuse.
\item True or False: The variance of the Maximum A Posteriori (MAP) estimate is generally lower than that of the Maximum Likelihood Estimate (MLE).
\item True or False: Neural networks can only be trained using deterministic optimization methods, excluding stochastic approaches.
\item True or False: The joint probability P(A, B, C) can be expressed as P(A, B | C) * P(C).
\end{enumerate}

\section*{Long Form Response (20 marks)}
\textit{Answer each question with a clear and complete explanation.}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{10}
\item Discuss the effectiveness of convolutional networks in classifying high-resolution images as of 2020, and analyze their advantages over other architectures in this context.
\item Discuss how unsupervised learning can be applied to analyze a large dataset of medical records from heart disease patients, and explain its significance in identifying distinct patient clusters for tailored treatments.
\end{enumerate}

\vfill
\noindent\textit{End of Paper}
\end{document}