\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\geometry{margin=1in}
\begin{document}
\begin{center}
Computer Science - K-12 Level Assessment 4 \
Total Marks: 40 \
Answer all questions.
\end{center}

\section*{Multiple Choice (10 marks)}
\begin{enumerate}[label=\arabic*.]
\item Which is a valid expression in Python 3.5?
\begin{enumerate}[label=(\alph*)]
    \item sort('ab')
    \item sorted('ab')
    \item "ab".sort()
    \item 1/0
\end{enumerate}
\item Which of the following best explains what happens when a new device is connected to the Internet?
\begin{enumerate}[label=(\alph*)]
    \item A device driver is assigned to the device.
    \item An Internet Protocol (IP) address is assigned to the device.
    \item A packet number is assigned to the device.
    \item A Web site is assigned to the device.
\end{enumerate}
\item Let l = [1,2,3,4]. What is sum(l) in Python 3?
\begin{enumerate}[label=(\alph*)]
    \item 4
    \item 1
    \item 10
    \item 8
\end{enumerate}
\item In Python 3, what is ['a', 'Chemistry', 0, 1][-3]?
\begin{enumerate}[label=(\alph*)]
    \item a
    \item Chemistry
    \item 0
    \item 1
\end{enumerate}
\item Which of the following spreadsheet functions would be most useful for detecting improbably high or low values that have become part of a data set as a result of data entry errors?
\begin{enumerate}[label=(\alph*)]
    \item A function that averages numeric values in a column or row
    \item A function that counts the values in a column or row
    \item A function that rounds a numeric value
    \item A function that sorts values in a column or row
\end{enumerate}
\end{enumerate}

\section*{True / False (10 marks)}
\textit{Answer True or False}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{5}
\item True or False: In Python 3, using b[::2] on the list [11,13,15,17,19,21] returns [19,21].
\item True or False: O(log n) grows faster than O($n^2$) as the input size increases.
\item True or False: In Python 3, the random() function sets the integer starting value used in generating random numbers.
\item True or False: In Python 3, shifting the number 1 to the left by 3 bits results in the value 8.
\item True or False: In Python 3, the strip() function removes all leading and trailing whitespace from a string.
\end{enumerate}

\section*{Long Form Response (20 marks)}
\textit{Answer each question with a clear and complete explanation.}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{10}
\item Discuss how the `isupper()` function in Python 3 determines if all characters in a string are uppercase. Provide examples to illustrate its usage and potential applications.
\item Explain how the lambda function r = lambda q: q * 2 works in Python 3, and analyze the output of r(3). What does this reveal about the use of lambda functions?
\end{enumerate}

\vfill
\noindent\textit{End of Paper}
\end{document}